// Write a function rotateArray(arr: number[], k: number): number[]
// that takes an array of numbers arr and a non-negative integer k,
// and returns a new array resulting from rotating arr to the right by k positions.
// For example, if arr = [1, 2, 3, 4, 5] and k = 2, your function should return [4, 5, 1, 2, 3]